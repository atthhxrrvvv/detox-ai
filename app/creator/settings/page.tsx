import { PageShell } from "@/components/PageShell";

export default function CreatorSettingsPage() {
  return <PageShell eyebrow="Creator" title="Creator settings" description="Manage announcement text, maintenance mode, signup availability, default model, UPI ID, and privacy notice version." />;
}

