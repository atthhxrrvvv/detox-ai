export type DetoxChat = {
  chatId: string;
  userId: string;
  userEmail: string;
  title: string;
  modelId: string;
  backendModel: string;
  mode: string;
  createdAt: string;
  updatedAt: string;
  pinned: boolean;
  archived: boolean;
  isReported: boolean;
  isDeleted: boolean;
};

